#include <stdio.h>
#include <stdlib.h>

int main()
{
    int x,y,total;
    printf("Enter two values:");
    scanf("%d %d", &x,&y);
    total=x+y;
    printf("Total value is %d", total);
    return 0;
}
